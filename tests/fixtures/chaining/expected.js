angular.module('app', [])
  .config(['a', function (a) {
  }])
  .controller('SomeCtrl', ['a', function (a) {
  }])
  .directive('someDirective', ['a', function (a) {
  }])
  .provider('someProvider', ['a', function (a) {
  }]);
