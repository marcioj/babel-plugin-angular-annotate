angular.module('app', [])
  .config(function (a) {
  })
  .controller('SomeCtrl', function (a) {
  })
  .directive('someDirective', function (a) {
  })
  .provider('someProvider', function (a) {
  });
